name: Homebrew
on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
    - name: Disabled
      run: echo "Homebrew publishing moved to trajano/homebrew-tap."
