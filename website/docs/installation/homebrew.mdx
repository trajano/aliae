import useDocusaurusContext from "@docusaurus/useDocusaurusContext";
export const customFields = () => useDocusaurusContext().siteConfig.customFields

A [Homebrew][brew] Formula and Cask (macOS only) are available for easy installation.

> Note: these Homebrew commands are for the <code>{customFields().repositorySlug}</code> fork, not the official `JanDeDobbeleer/aliae` repository.

:::warning Linux
When installing Homebrew for Linux,
be sure to follow *[Next steps][nextsteps]* instructions to add Homebrew to your PATH and to your
bash shell profile script, and *[Requirements][requirements]* to build aliae.
:::

```bash
brew install trajano/aliae/aliae
```

## Update

```bash
brew update && brew upgrade aliae
```

[brew]: https://brew.sh
[requirements]: https://docs.brew.sh/Homebrew-on-Linux#requirements
[nextsteps]: https://docs.brew.sh/Homebrew-on-Linux#install
